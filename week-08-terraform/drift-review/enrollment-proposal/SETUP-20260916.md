# Eze Favour — temporary enrollment setup, 16 September 2026

**AUTHENTICATION AND BEDROCK ENROLLMENT VERIFIED AT THE TIMES BELOW — TEMPORARY ACCESS NOW EXPIRED; CLAUDE REVIEW STILL PENDING.**

The [sanitized continuation record](../reports/continuation-20260916.json) records genuine MFA authentication, user-triggered offer acceptance, independent availability, the separate review-budget approval, and a later unapplied network plan. Earlier authentication blockers below are historical, not the current enrollment status.

The original proposal expired at 00:45 UTC. Codex prepared the same five policy
statements with a new fixed expiry of **2026-09-16 13:30 UTC (14:30 Lagos)** and
presented that change for review. The user created the policy in the AWS console
and reported "i have created". The console confirmed creation at 12:26 UTC+01.
This records a human-created IAM policy, not human approval of the assignment's
Terraform resolution or acceptance of model terms.

## Verified configuration

- Managed policy: `DMIWeek8HaikuEnrollment20260916`.
- [Saved policy snapshot](haiku-enrollment-policy-20260916.json): all five saved
  statements were read back through the console and matched the reviewed JSON.
  Only the five expiry values differ from the [original draft](haiku-enrollment-policy.json).
- Codex used the setup browser session to create `dmi-week8-enrollment`; AWS
  recorded creation at 12:29 UTC+01 and returned success.
- User-page read-back showed exactly one directly attached permissions policy,
  the policy above, and the same named policy as the permissions boundary.
- Creation generated no password or access key. Console access was disabled;
  there was no forced password reset. Credential and MFA setup belong to the user.
- This work did not modify the lab user's policy or the Claude runtime role.

The [original scope and limitations](README.md) still apply: Mumbai/MFA-gated
Bedrock entry points; service-mediated Marketplace prerequisites; only the
Haiku product ID for subscription. Agreement creation is an account-level
permission, and service-mediated unsubscription cannot be product-scoped. The
policy grants no inference or infrastructure access. Its expiry limits these
grants; it does not delete the identity or revoke an established agreement.

## Local readiness, not live model evidence

An isolated official-PyPI boto3/botocore 1.43.95 environment was prepared in
ignored local scratch space. Offline SDK inspection confirmed the availability
and offer API input/output shapes. A synthetic identity-guard check accepted the
expected same-account user pair and rejected five invalid cases, including root,
wrong user, and mismatched accounts. These were local checks, not AWS IAM tests.

The private read-only helper permits identity, availability, and offer reads;
it does not invoke models or create/delete agreements. Its deadline now matches
the saved policy. AWS CLI 2.26.1 lacks the required agreement commands and also
lacks `aws configure mfa-login`; no global CLI or Claude settings were changed.

## Historical continuation check — 16 September 2026, 11:43 UTC

GitHub Copilot verified that local and remote working-branch HEAD matched
`976212b` and the worktree was clean. PR #2 had already merged at
**11:13:58 UTC** as `d244042`; setup commit `976212b` was pushed afterward and is
not included in that merged PR. No further merge is authorized by this handoff.

The reviewed window was still open at the **11:43 UTC** clock check, but local
profile discovery found no `dmi-week8-enrollment` profile. After implementation
inspection, the read-only helper's `identity` action returned **BLOCKED**, reason
`Required named profile is not configured`, exit **3**, before its AWS identity
requests. This establishes an authentication prerequisite, not failed model
availability or a need for broader permissions.

At that checkpoint MFA completion was **unverified**. The user was unavailable
to confirm or complete private authentication, so that continuation stopped at
that gate. The IAM setup above is the earlier Codex read-back, not a fresh IAM verification.
No IAM change, agreement acceptance, subscription, model invocation or Terraform
operation was performed during this continuation. The deadline is unchanged;
expiry requires a separately reviewed renewal before enrollment can proceed.

That checkpoint's offline regression/evidence-integrity validation passed
**43 tests and 217 subtests**, with **54 source hashes**. The
[validation record](../reports/local-validation.json) is regenerated for later
changes and is not a rerun of cloud operations. At the checkpoint all 14
screenshot/source pairs were unchanged; $0.18 remained reserved, not confirmed
charges. Those offline tests did not verify MFA or enrollment.

## Verified private authentication and agreement acceptance

All times below are UTC on 16 September; Lagos is one hour ahead.

- **12:20:53:** the privately configured base profile authenticated as the exact
  expected same-account non-root enrollment user. Identifiers were withheld.
- **12:48:03:** the user created the MFA-authenticated temporary session in their
  own Terminal. It expired at **13:29**; the policy expiry stayed **13:30**.
- **12:50:02:** authenticated read-only availability returned agreement
  `NOT_AVAILABLE`, authorization `AUTHORIZED`, entitlement and region `AVAILABLE`.
  The actual offer was then retrieved privately without accepting it.
- The actual offer's legal PDF was downloaded privately from AWS, hash-verified
  and opened in Preview. Signed URLs and offer tokens were not published.
- The user explicitly approved acceptance of that specific offer only. Fresh
  identity, session/permission windows, offer contents and PDF hash were checked
  again before acceptance; no terms substitution or silent renewal was allowed.
- **13:26:22:** the user-triggered local SDK helper received private Terminal
  confirmation and returned `ACCEPTANCE_RESPONSE_CONFIRMED`. This was **not a
  manual console acceptance**. Immediate agreement availability was `PENDING`.
- **13:27:31:** an independent read-only check returned agreement `AVAILABLE`,
  authorization `AUTHORIZED`, entitlement `AVAILABLE`, and region `AVAILABLE`.
  Acceptance was not repeated. No inference or provisioning occurred.

The [sanitized record](../reports/continuation-20260916.json) includes hashes of
the actual private acceptance and availability records and the approved terms.
The MFA helper and acceptance helper each passed **17 focused offline tests**;
those mocked tests are separate from the genuine AWS results above.

## Current continuation gates

At **13:30:22.429 UTC**, the user separately approved **up to $0.32 of the existing
$0.50 allowance** for global Haiku reviews and the safe hook demonstration.
The **$0.18 unknown-usage reservation remains unchanged**; it is not a confirmed
charge. This did not authorize new infrastructure, human Terraform resolution,
account changes, publication, or another merge. A CLI threshold is not an AWS
account spending cap. The historical $0.00017 connection charge is separate.

The enrollment session and fixed policy window are now expired. They were not
extended or reused after expiry. Expiry does not delete the IAM identity,
remove its access key or detach its policy; removal is not claimed. Any removal
or renewal requires its own reviewed authorization. The separate restricted
Bedrock runtime role passed identity-only verification, not a post-enrollment
inference test.

At **13:44:17 UTC**, a fresh read-only lab preflight verified the exact non-root
source identity, empty isolated states and zero tagged VPCs/security groups.
A saved network plan proposed **one VPC create, zero updates/deletions** and was
not applied. A security-group create plan still requires a newly approved VPC;
the deleted VPC binding was not reused. No new deployment is approved.

Installed Claude Code **2.1.220** and the official hook reference support the
existing `command` plus `args` exec form. Configuration/schema checks are not
native `PreToolUse` execution evidence. Before inference, enforce durable shared
budget accounting and preserve all existing unknown-usage reservations.
Fresh provisioning/cleanup approval, actual clean/risk/final reviews, and a
genuine human resolution decision are still required. All 14 screenshots remain
unchanged; **10, 12, 15, 16 and 17** remain pending. No new publication is claimed.

# Week 04 Completion Report — Git and GitHub

## Project status
- `CodeTrack` project exists in the repo with:
  - `CodeTrack/index.html`
  - `CodeTrack/style.css`
  - `CodeTrack/contact.html`
- `CodeTrack/index.html` now includes:
  - Student: `Favour Eze`
  - Group: `Group 3`
  - a working `Contact Us` link to `contact.html`
- `hooks/pre-commit` is present and contains a tracked Git pre-commit hook.
- `.claude/skills/pr-ready/SKILL.md` is present and correctly restricts tools to `Bash, Read, Grep` with `disable-model-invocation: true`.
- `week-04-git-and-github/risky_config.py` exists and is appropriate for the AI safety-net assignment.

## Assignment summary

### Assignment 1 — Initial Git Setup
- Completed in markdown and evidence linked using `screenshots/assignment1-git-init.png`.
- Local Git initialization, `.git` folder verification, local config, and global config are all documented.

### Assignment 2 — Tracking, staging, committing, EC2 deploy
- Completed in markdown and evidence linked using `screenshots/assignment2-git-workflow.png`.
- `index.html` and `style.css` are created.
- Student name and group name were added.
- Deployment task evidence is still pending live verification (EC2/Nginx external site), but local CodeTrack project is configured.

### Assignment 3 — Branching workflow + Contact page
- `contact.html` exists in `CodeTrack`.
- `index.html` now includes a `contact.html` link.
- Assignment markdown is present, but screenshot image references are not currently embedded.
- Additional evidence images exist in `week-04-git-and-github/screenshots/` with names like `Screenshot assign9...` that may correspond to this assignment.

### Assignment 4 — GitHub account and profile
- Markdown is present with completed narrative evidence for GitHub account `Favourcloud`.
- Screenshot image references are not currently embedded in the assignment markdown.

### Assignment 5 — Fork/clone/sync/pull-request
- Markdown is present and describes the full workflow.
- Evidence narrative is included, and the report notes a fork URL and PR URL in the assignment file.
- Screenshot image references are not currently embedded in the assignment markdown.

### Assignment 6 — AI-assisted Git safety net
- Hook file `hooks/pre-commit` exists and is tracked.
- Skill file `.claude/skills/pr-ready/SKILL.md` exists and is configured correctly.
- `week-04-git-and-github/risky_config.py` exists as the staged-risk file.
- One screenshot file exists: `screenshots/assignment6-hook-test.png`.
- Assignment markdown is present but lacks screenshot image embeds.

## Evidence files found
- `week-04-git-and-github/screenshots/assignment1-git-init.png`
- `week-04-git-and-github/screenshots/assignment2-git-workflow.png`
- `week-04-git-and-github/screenshots/assignment6-hook-test.png`
- A set of additional images with names like `Screenshot assign9...` that likely belong to assignment 2/3/6 workflows.

## Recommended next actions
1. Add screenshot image links to `assignment-03-codetrack-branching-workflow-contact-page.md` and `assignment-06-ai-assisted-git-safety-net.md` once the correct files are identified.
2. Add screenshot image links to `assignment-04-github-account-and-professional-profile.md` and `assignment-05-github-fork-clone-sync-pull-request.md` as evidence is finalized.
3. Optional: rename the `Screenshot assign9...` files to cleaner names that match assignment deliverables.
4. Verify EC2/Nginx deployment status if you want to complete Assignment 2 fully.

## Final notes
- The repository is on `main` and currently includes the `CodeTrack` project with the required pages.
- The Git safety net assignment is present and structurally complete in the repo.
- The remaining gap is primarily formal evidence linking inside the markdown files, not implementation.

# Intentionally unconfigured. Do not invent or commit an IP, user key path or credentials.
# After approval and a real apply, use the private inventory described in ../README.md.
[web]

[controller]
localhost ansible_connection=local

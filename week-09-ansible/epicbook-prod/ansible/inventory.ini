# Deliberately empty: never substitute localhost or a documentation IP for a VM.
# After authorization/provisioning, scripts/render_inventory.py writes inventory.local.ini.
# site.yml rejects this empty template BEFORE contacting any managed host.
[web]

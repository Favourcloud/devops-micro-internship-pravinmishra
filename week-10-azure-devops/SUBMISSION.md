# Week 10 submission handoff — Eze Favour

**Prepared 19 September 2026 · Partial submission, not a claim that Week 10 is complete.**

> **Later screenshot supplements:** [four numbered A1/A2 slots now have genuine attachments](evidence/README.md), using six original PNGs. A2 slot 3 is source-only YAML evidence in three views; its SSH connection remains unset. Human visual/privacy review is pending. The zero-image counts and test results below describe the original **08:15:30 UTC** snapshot, before those captures; use the supplement for current image availability.

This handoff collects genuine recorded progress and tested source. A1 had a
successful authorized technical trial; A2 reached the Azure Repos/source-handoff
milestone. A2–A4 live deployments and A5 triage remain unfinished. All required
screenshots and learner reflections remain pending. The original briefs—not this
report—define the assessment requirements.

The technical operations described below were assistant-operated under specific
user authorization. They are not claims of learner-performed steps. This
submission-preparation pass used no cloud credentials, cloud operations, SSH,
application builds, GUI capture or social publication. Its status is based on
committed historical receipts, not a new live inventory.

## Start here

| Assignment / original brief | Delivered material | Still required |
| --- | --- | --- |
| [A1 — Self-hosted Ubuntu agent](assignment-01-set-up-a-self-hosted-linux-agent-for-azure-devops.md) | [Runbook and manual YAML](self-hosted-agent/README.md), [Azure VM source](self-hosted-agent/azure-vm/README.md), [successful trial/cleanup receipt](self-hosted-agent/runtime-2026-09-18.json) | Seven genuine screenshots, private PAT scope/expiry/revocation confirmation, learner notes and checklist review; fresh authorization/host for any repeat |
| [A2 — Static website](assignment-02-deploy-a-static-website-to-aws-ec2-using-an-azure-devops-cicd-pipeline.md) | [Verified import/personalization](application-pipelines/static-import-2026-09-18.json), [verified pipeline-source upload](application-pipelines/static-pipeline-source-2026-09-18.json), [deployment preparation](application-pipelines/README.md) | Agent and AWS target, restricted SSH connection, real manual/automatic pipeline runs and deployed site, five numbered screenshots, separate LinkedIn image/publication and learner notes |
| [A3 — React CI/CD](assignment-03-automate-react-app-deployment-using-azure-devops-cicd.md) | [Build/Test/Publish/Deploy template](application-pipelines/react.azure-pipelines.yml) and guarded target/helper source | Actual app import and authorized changes, application tests/build/artifact, target and deployment, automatic run, six screenshots and learner notes |
| [A4 — EpicBook dual pipelines](assignment-04-automate-epicbook-deployment-with-dual-pipelines.md) | [Handoff checks and source review](epicbook/README.md), [offline-tested workload Terraform](epicbook/terraform/README.md) | Two external repositories, protected remote state, both real pipelines, approved infrastructure/private MySQL, Ansible/application verification and manual handoff, six screenshots and learner notes |
| [A5 — Read-only failure triage](assignment-05-ai-assisted-azure-devops-dual-pipeline-failure-triage.md) | Original brief and recorded dependencies only | Actual supplied kit, A4's healthy two-pipeline baseline, authorized model use, genuine controlled failures/triage/recovery, twelve screenshots and learner notes |

## A1: genuine successful run and cleanup

- **Run:** [Azure DevOps manual run 1](https://dev.azure.com/aneneeze2021/DMI-Week10/_build/results?buildId=1&view=results), completed successfully **18 September 2026 at 10:30:20 UTC**.
- **Host/service:** Ubuntu 22.04, x86_64; agent 17 in pool `DMI-Week10-A1` (pool 11). The service ran as non-root `azdoagent`, and Online status was verified during the trial.
- **Actual output checked:** `uname -a`, `whoami` (returned `azdoagent`) and `df -h`.
- **Source revision used by the run:** `aef39187af3b62f0508f831b3282ac052ddb3dae`.
- **Task-log SHA-256:** `d387b7b6bdb4f77c9b81bcf77ec2c8e47c056bd8ccc9f3356d19eefa35043659`.
- **Cleanup:** service stopped/disabled/uninstalled; empty Terraform state and exact VM/resource-group/disk/IP absence verified at **10:41:37 UTC**. Agent removal and zero registered agents verified at **10:53:08 UTC**. No A1 host or agent was left running by that trial; its authorization is retired.

The [sanitized receipt](self-hosted-agent/runtime-2026-09-18.json) preserves those
observations. It is **not a screenshot**, a current health check or independent
proof generated by today's local tests. At the original snapshot, **0/7 captures**
were available; the [current seven-slot manifest](self-hosted-agent/evidence/manifest.json)
now records the later captures for slots 1 and 7. All **eight original checklist entries** remain
unchecked. PAT confirmation and the learner's own notes remain pending.

The Azure DevOps project is private. The assessor needs authorized project access
to verify the retained run. Do not share a PAT, password, login link or raw private
logs, and do not change project visibility to work around access.

## A2: actual repository/source milestone, not deployment

The private [Azure-Static-Website repository](https://dev.azure.com/aneneeze2021/DMI-Week10/_git/Azure-Static-Website)
was imported from instructor revision `258b8a380d45b030f29a1ace237b3f4d120820fd`.
The name-only HTML personalization was read back at commit
`8bfa3682a7440f12791085c1d4d0aa0f7fbcb4fb`. Instructor JavaScript was unchanged.

Azure commit `6b38993a18f75c2588803a42b6e7790c95e95b81` subsequently added only
`azure-pipelines.yml`, `ci/validate_site.py`, `ci/verify_remote.sh` and
`ci/ssh_tunnel.py`. Exact-parent, stored-hash and unchanged-application checks
succeeded. The handoff receipt records **zero application definitions/builds**,
an unset SSH connection identifier and **no deployment**. A YAML file in a
repository is not a successful pipeline run.

## Supporting access/recovery work is not assignment evidence

A persistent, narrowly scoped non-root identity, `dmi-week10-operator`, was
activated with MFA and no long-lived API keys. Historical verification on
19 September established CLI identity, protected IAM reads and a four-data-source
Terraform probe through a private credential-process bridge. It did not apply
workload infrastructure or establish complete deployment permission.

The [operator source](application-pipelines/cloud-cleanup/operator/README.md) and
[unarmed cleanup/recovery preparation](application-pipelines/cloud-cleanup/recovery/README.md)
remain supporting work. The original policy deadline is **19 September 2026,
18:18:03 UTC**; publication and submission do not renew it. Independent recovery,
protected-state custody/restore, actual federation, creator authority and complete
costs are still gates. The recovery reviewer is not an installed recovery service.

## Local validation — 19 September 2026

These are **credential-free source/unit tests**, not React tests, application
builds, successful Azure Pipelines runs or cloud acceptance tests.

| Suite | Tests passed |
| --- | ---: |
| A1 foundation | 43 |
| A1 Azure VM adaptation | 21 |
| A2/A3 pipeline/target/transport source | 145 |
| A4 EpicBook source/handoff | 51 |
| Cleanup/operator/recovery source | 58 |
| Submission consistency / brief preservation | 6 |
| **Total** | **324** |

All six suites passed. The [machine-readable snapshot](submission/status.json)
records brief hashes, source receipts, pending evidence and actual validation
counts. Historical native Terraform mock/schema results remain in their original
runbooks; those native suites were not rerun for this documentation package.

Reproduce from the **full repository root**, using macOS's existing Python and
sandbox tooling; no install, credentials or cloud access is needed:

```sh
for suite in \
  week-10-azure-devops/self-hosted-agent/tests \
  week-10-azure-devops/self-hosted-agent/azure-vm/tests \
  week-10-azure-devops/application-pipelines/tests \
  week-10-azure-devops/epicbook/tests \
  week-10-azure-devops/application-pipelines/cloud-cleanup/tests \
  week-10-azure-devops/submission/tests
do
  /usr/bin/env -i PATH=/usr/bin:/bin HOME=/nonexistent \
    /usr/bin/sandbox-exec -p \
    '(version 1)(allow default)(deny network*)(deny file-write*)(allow file-write-data (literal "/dev/null"))' \
    /usr/bin/python3 -I -B -m unittest discover -s "$suite" -p 'test_*.py' -q || exit
done
```

Each suite runs in a separate process. The sandbox denies network access and
ordinary-file writes; `/dev/null` is the discard-only exception. Python uses
isolated mode and writes no bytecode. The complete checkout is required because
some tests and runbooks reference earlier, reused repository source. A Week10-only
source archive intentionally omits that external context, all ignored/private
files, credentials, state, plans, caches and provider binaries.

## Submission and remaining evidence

1. Submit this report with the reviewed Week10 source and the retained A1 run
   link, identifying the work as **partial**. The receipts explain exactly which
   technical operations occurred and which did not.
2. Provide the assessor authorized access to the private Azure DevOps run/repo
   through the institution's normal process; never send authentication material.
3. Include only genuine, readable captures already authorized and actually taken.
   There are currently **0/36 numbered screenshots**, plus **0/1 separate A2
   LinkedIn image**. A successful historical trial cannot recreate missing
   screenshots of a host that was cleaned up.
4. Complete the learner's own notes and review original checklists against
   genuine evidence. No reflections have been written on the learner's behalf.
5. Arrange additional time for the remaining live work rather than presenting
   templates, mock tests, synthetic images or fictional incidents as completion.

A5's required `CLAUDE.md`, `pipeline-triage.sh` and
`.claude/skills/pipeline-triage/SKILL.md` were absent from the previously inspected
instructor source. No substitute kit or fabricated incident is included.

All five published briefs are byte-preserved by this packaging change. Their
original screenshot slots, completion criteria and learner-note sections remain
intact. No new cloud permission, screenshot approval or social-post authorization
is granted by this document.

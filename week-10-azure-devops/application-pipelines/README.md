# Week 10 — static and React pipeline delivery

Learner: **Eze Favour**. **A2's repository import, personalization, pipeline-source upload and historical restricted AWS session are recorded; deployment has not started. A3 remains source preparation. Neither assignment is complete.**

These are separately scoped follow-on files; see the [Week 10 status and remaining gates](../README.md) for the combined delivery. No application dependency installation/build/test, SSH service connection, application pipeline run, deployment or social publication is claimed here. The later [source-view captures](../evidence/README.md) are distinct from those unperformed operations. [Source coordinates](sources.json) remain the historical public-source review; the separate [A2 import receipt](static-import-2026-09-18.json) records the actual subsequent repository operations.

The [earlier readiness snapshot](target/terraform/README.md#actual-readiness-snapshot--18-september-2026) and [16:47 UTC receipt](readiness-2026-09-18.json) retain the previous failed methods and HTTP 401 responses. After the user edited the existing PAT, **repository-list and SSH service-connection-list reads succeeded at 16:58 UTC**, using the saved credential without another handoff. There were zero Online agents and no SSH connections. Repository creation, import and the guarded HTML commit then succeeded; this does not prove service-connection write permission or independently verify exact PAT scopes/server expiry. No account membership, role or billing change was made.

**Live deployment remains blocked pending fresh authorization, independent cleanup and verified transport/access.** The [runtime authorization gate](../README.md#runtime-authorization-remains-separate) classifies the earlier 24-hour/US$10 window as a **historical proposal**, not current permission. It does not authorize provisioning; separately reviewed scopes, exact inputs/plans, costs and cleanup are required. The reviewed Microsoft task sources do not configure a host verifier. A [host-pinned loopback transport](transport/README.md), restricted forwarding account, agent preparation and pipeline lifecycle are now implemented and tested offline. They retain the required native tasks, but are not installed, live-verified or approved deployment configurations; see the [transport finding](#ssh-transport-review--18-september-2026).

| Brief | Prepared source | Still required |
| --- | --- | --- |
| [A2 — static site](../assignment-02-deploy-a-static-website-to-aws-ec2-using-an-azure-devops-cicd-pipeline.md) | Verified Azure Repos import/name-only HTML commit and four-file pipeline-source upload; historical restricted non-root AWS session; prepared all-branch CI, runtime-selected Linux pool, payload-only `CopyFilesOverSSH@0`, `SSH@0`, guarded Ansible and AWS Terraform source | Freshly approved agent registration (credential permissions/scope/expiry still require verification), independently proven workload cleanup, full deployment permissions, authenticated native-task transport, approved real Terraform/Ansible execution, restricted SSH connection, successful manual **and automatic** deployments, live grading window, remaining screenshots 2/4/5, human review of four existing PNGs and separate LinkedIn image/URL |
| [A3 — React](../assignment-03-automate-react-app-deployment-using-azure-devops-cicd.md) | Main CI, Build → Test → Publish → Deploy, same-run artifact promotion, checksums, SPA/HTTP verification, guarded Ansible and mock-tested Azure Terraform target source | Authorized application personalization/test correction, separate approved real Terraform/Ansible execution, real four-stage success and automatic trigger, browser verification, six images including LinkedIn |

## Current A2 handoff — 19 September 2026

- **Evidence already available:** [A1 has all seven captures](../self-hosted-agent/evidence/manifest.json), with [user-attested image review](../evidence/a1-human-review-2026-09-19.json). Its temporary agents and VMs were removed; no Online agent is carried forward into A2. A1's historical Tasks 2–6 and notes item are now checked, the latter through [user approval of the assistant-drafted reflection](../evidence/a1-reflection-approval-2026-09-20.json); PAT compliance and the universal secrecy assertion remain unresolved. A2 retains four source-only checks for import, visible `index.html`, name added to source and the all-branch YAML trigger; none establishes a deployment or reviewed images. See the [bounded checklist reconciliation](../evidence/README.md#checklist-reconciliation-from-existing-records).
- **A2 has two of five numbered slots:** screenshot 1 and screenshot 3 contain four original PNGs in the [brief](../assignment-02-deploy-a-static-website-to-aws-ec2-using-an-azure-devops-cicd-pipeline.md). All four still need human review. Screenshot 3 is three source views for one slot, with an unset connection UUID—not a configured connection or successful run. Slots **2, 4 and 5**, the separate LinkedIn image and qualifying published URL remain missing.
- **Do not reuse expired authority:** the recorded AWS bootstrap privilege deadline was **19 September 2026, 18:18:03 UTC**. It has passed; the root authorization is retired. A new login, this handoff or the earlier maximum 24-hour proposal cannot renew that grant or authorize provisioning. Follow the [operator](cloud-cleanup/operator/README.md) and [recovery](cloud-cleanup/recovery/README.md) gates for separately reviewed authority, independent hosting/state custody, full costs and teardown.
- **Next live sequence:** first accept independent recovery and canary creation/deletion/control-plane teardown. The [separate A2 cleanup executor](cloud-cleanup/workloads/README.md) is now implemented and tested offline, but remains disabled and uninstalled. Review its independent hosting, external-state integration and workload-specific permissions; prove them in a separately approved workload canary before starting the assignment deployment. Only after fresh approval may a new agent, AWS target, Ansible configuration, host-pinned native-task transport and restricted SSH connection be established. Actual manual and pushed-commit deployments, public-site verification and the agreed grading/retention window precede the remaining captures. No live gate is satisfied by a source test or a readiness boolean.

The shared PAT is to remain unchanged for this continuation. No token revocation, scope/expiry change, authentication handoff, cloud operation, screenshot capture or social publication is authorized by this source update. Historical receipts and all original requirements remain unchanged.

## Before configuring either pipeline

1. Complete [A1's actual agent gates](../self-hosted-agent/README.md). A pool's existence does not mean an agent is Online. Use only a dedicated, unprivileged agent and reviewed code in the private project. Do not run untrusted fork PRs. `pr: none` is not an Azure Repos branch-policy or Classic-pipeline security control: review those separately. Authorize only each exact pipeline to its pool and SSH connection; never grant all pipelines access.
2. Obtain fresh identity, cloud scope, price/budget, lifetime, cleanup and evidence permissions. A1's temporary Azure-agent allowance does **not** authorize A2's AWS host or A3's host. A2 specifically requires AWS and that its EC2 instance remain running during grading; a grading/retention window has not been agreed. Never operate targets or pipelines as AWS root. The [later restricted-session check](#actual-aws-session-follow-up--18-september-2026) verified an authorized non-root identity and tagged VPC dry-run permission, not every apply/destroy permission or live cleanup. The earlier read-only session remains a separate historical result.
3. A2's instructor repository is already imported into the approved private **Azure Repos** project, with its pinned source and name-only commit verified below. Do not re-import or overwrite it. A3's import remains pending. Review the actual repository and commit/ref again before enabling CI; GitHub updates do not synchronize either Azure repository. Recheck the [pinned public source facts](sources.json) if upstream changed.
4. A2's exact YAML/helpers were [already uploaded and verified](#actual-a2-pipeline-source-handoff--18-september-2026); do not overwrite or re-upload them blindly. For A3, copy `react.azure-pipelines.yml` to the **imported application's** root as `azure-pipelines.yml`, with [ci/validate_site.py](ci/validate_site.py), [ci/verify_remote.sh](ci/verify_remote.sh) and [ci/ssh_tunnel.py](ci/ssh_tunnel.py) unchanged in `ci/`. Do not point these application pipelines at this assessment repository. No instructor JavaScript is vendored, edited or executed here.
5. Complete the [ordered agent/target transport preparation](transport/README.md#ordered-human-authorized-preparation) under fresh live authorization. Replace the SSH service-connection placeholder with its reviewed canonical **UUID**, matching the root-owned agent profile, not a secret. Verify the saved connection uses `127.0.0.1`, port `22222` for A2 or `22223` for A3, and user `week10deploy`. Select the approved pool at queue time; its default name does not prove readiness. For React, set `deploymentDate` to the actual intended date, `YYYY-MM-DD`, matching the personalized app. Keep `learnerName: Eze Favour` and `targetFolder: /var/www/html`.

## Actual starter constraints — do not conceal them

- A2's source contains `README.md` and `index.html`. The HTML includes an upstream inline script. Add the name in the HTML body without modifying that instructor script. Only the reviewed HTML and generated checksum manifest are staged, never the repository, `.git`, pipeline definitions or credentials. External script/stylesheet dependencies need a separately reviewed payload contract.
- A3 uses React 19 / `react-scripts` 5.0.1. Its documented name/date fields are in **`src/App.js`**. Changing them is outside this preparation's no-JavaScript-edit boundary and remains a separately authorized application-repository task. A terminal caption or an added screenshot label is not application personalization.
- The starter's `npm test` is `react-scripts test tests`: it filters tests to `src/tests/App.test.js`. Another file, `src/App.test.js`, expects “learn react”, which is absent from the reviewed component. This is a **source-review finding**, not a reported test execution. The pipeline deliberately invokes the local test runner without that filter. It is expected to expose the stale assertion; a real run is still required. Review/fix the test to match intended behavior under appropriate authorization. Do not delete tests, hide the failure, use `--passWithNoTests`, or claim all tests passed after selecting only the convenient tests.
- Node **22.23.2** is an actual published version; its official [checksum listing](https://nodejs.org/dist/v22.23.2/SHASUMS256.txt) was read without downloading a package. `NodeTool@0` uses that exact version in both Build and Test. This does not claim independent archive verification by NodeTool. Review the release/support status before live use; do not silently upgrade the application lockfile.

## Reproducible static personalization

[ci/personalize_static.py](ci/personalize_static.py) accepts only the exact `index.html` SHA-256 recorded in [sources.json](sources.json). It emits one additional **Learner: Eze Favour** paragraph inside the existing header and leaves every other source byte unchanged, including the instructor's inline script. It neither fetches a repository nor writes its input, invokes Git/cloud tools, executes JavaScript or deploys anything. An upstream change or already-personalized input is rejected rather than silently patched.

Use an unchanged file from the pinned instructor commit and a **different, new output file** in an approved private staging directory. Never redirect stdout onto the input. For example, these are prospective paths, not an import or execution record:

```sh
umask 077
set -C
env -i PATH=/usr/bin:/bin HOME=/nonexistent \
  /usr/bin/python3 -I -B week-10-azure-devops/application-pipelines/ci/personalize_static.py \
  /approved/unchanged/index.html > /approved/candidate/index.html
```

The real pinned 15,814-byte source was fetched and its recorded digest verified on 18 September. The resulting 15,882-byte candidate passed the static payload contract; removing the single added paragraph reproduced the original bytes, and the one script block was unchanged. Its digest is in the [16:47 readiness receipt](readiness-2026-09-18.json). That local transformation was not itself an import or deployment; the candidate was subsequently committed to Azure Repos as recorded below. The HTML is not vendored into this assessment repository. Do not upload the transformation helper or the source repository as website content.

## Actual A2 repository milestone — 18 September 2026

The authorized assistant created [`DMI-Week10/Azure-Static-Website`](https://dev.azure.com/aneneeze2021/DMI-Week10/_git/Azure-Static-Website) in the existing **private** project at 17:04 UTC, after confirming the intended repository did not already exist. Azure import request **2** completed; its only branch, `main`, exactly matched public instructor commit `258b8a380d45b030f29a1ace237b3f4d120820fd`. The public source was rechecked before import and contained only `README.md` and `index.html`.

A compare-and-swap push against that exact parent created commit **`8bfa3682a7440f12791085c1d4d0aa0f7fbcb4fb`**. Independent reads verified the remote HTML digest, unchanged script/source bytes apart from the learner paragraph, the exact parent and a Git diff containing only one edit to `/index.html`. These are actual repository writes, not inferred write permission from successful GETs. The [sanitized import receipt](static-import-2026-09-18.json) records the separate observations without credentials or private local paths.

At that import check, no pipeline definition existed for this application repository. No pipeline YAML/helpers had yet been added there, no CI was enabled or queued, and no application code was executed or rendered. Nine credential-free tests of the session-only operation guards passed with network and filesystem writes denied before the writes. They are not application tests. The controller credential stayed local and was not forwarded to GitHub or an agent. No SSH connection, cloud host, screenshot, learner reflection or social post was created. At that check, the native-task transport fix and fresh paid-resource authorization were still missing. The subsequent transport implementation and 24-hour approval do not remove the remaining identity, plan and live-acceptance gates.

## Actual A2 pipeline-source handoff — 18 September 2026

A separate exact-parent push added `azure-pipelines.yml` and three `ci/` helpers from tested GitHub revision `a9930bcd39168906681de4b7c30ebc0cba09eebf`. Azure commit **`6b38993a18f75c2588803a42b6e7790c95e95b81`**, push **4**, was independently verified by **18:23 UTC**. Its parent is the personalized HTML commit; all four stored source hashes match, and the original README/index Git blobs are unchanged. Azure's changes API also reports the added `/ci` directory; the verification explicitly checks that directory plus four files, not five application files. See the [allowlisted receipt](static-pipeline-source-2026-09-18.json).

Fresh checks before the push and during verification found **zero application pipeline definitions and zero builds**. The separate A1 definition/repository and manual YAML were unchanged. A2's source includes its intended all-branch CI trigger, but **no definition was created, enabled or queued**. The SSH connection UUID remains an unset placeholder. Do not enable execution until the real agent, connection, transport and target gates pass. Sixteen credential-free operation-guard tests passed with network/filesystem writes denied, except discard-only `/dev/null` access for Git; these are not application tests.

## Actual AWS session follow-up — 18 September 2026

After explicit root-use permission, the controller used root only to verify the issuer account and request a restricted STS federated session. The returned **non-root** identity was independently verified. No IAM user, role or permanent access key was created; root was not passed to Terraform, an agent or a pipeline. AWS limits root-issued sessions to **one hour**, separately from the approved 24-hour resource window. This session's immutable expiry is **19:27:20 UTC**; any renewal must stay within the current authorization and cannot move the resource-cleanup deadline.

At **18:27 UTC**, a tagged `CreateVpc --dry-run` returned `DryRunOperation`; untagged creation and an EC2 read in another region returned `UnauthorizedOperation`. The pinned Canonical AMI remained available, the standard vCPU quota was 5, and the A2-tagged VPC list was empty. The policy includes explicit action/region/tag restrictions, but it is **not unique-prefix isolation**, a complete permission proof, a live cleanup test or a spending cap. All VPC probes retained `--dry-run`; no resource or live Terraform plan was created. See the [sanitized AWS receipt](aws-session-readiness-2026-09-18.json). Sixteen offline broker tests passed; their synthetic policy evaluator is not AWS IAM validation.

The root-only sign-in blocker was resolved for that historical scoped session, not indefinitely. The user subsequently explicitly authorized reuse of the existing saved PAT for the next agent registration; another token is not a prerequisite. A later protected read verified the expected user/project/pool, but successful reads do not prove registration permission, exact PAT scopes or server expiry. Recheck those gates privately at execution time. Do not print, export, commit or forward the PAT to cleanup; use it only through the approved private registration handoff. Complete apply/destroy and renewal/cleanup readiness, actual agent egress, exact plans/costs, authentic host keys and live transport acceptance remain required. No resource clock has started.

## Independent cleanup preparation

The user selected cloud-hosted cleanup, independent of the controller's uptime. The [canary project](cloud-cleanup/README.md) provides unarmed hosted-pipeline source, narrowly federated bootstrap Terraform, protected canary state and delete-only checks for one empty Azure resource group and AWS VPC. That worker cannot clean up A2 workload VMs. A [separate disabled workload executor](cloud-cleanup/workloads/README.md) now prepares, validates and applies saved destroy plans for the exact A2 AWS target and reused Azure agent, with external-state and live-inventory gates. No service, permissions or backend has been installed or changed by this source delivery. Offline tests are not proof of scheduling, federation or deletion.

No cleanup connection, definition, schedule, identity, storage account or canary was created or armed in this delivery. The current STS-only broker cannot administer IAM: a separately authorized non-root bootstrap identity and a verified control-plane teardown path are required before live bootstrap. Then actual canary acceptance must precede separately reviewed A2 workload permissions/state integration. Keep the same fixed maximum 24-hour window and combined US$10 planning allowance, including control-plane costs; neither canary success nor credential renewal starts a new clock.

## SSH transport review — 18 September 2026

The public Microsoft task source was reviewed at commit [`b7a1e04faa09a7028a67c9ac6d8c60c78153a6da`](https://github.com/microsoft/azure-pipelines-tasks/tree/b7a1e04faa09a7028a67c9ac6d8c60c78153a6da):

- [`SSH@0` source, version 0.279.0](https://github.com/microsoft/azure-pipelines-tasks/blob/b7a1e04faa09a7028a67c9ac6d8c60c78153a6da/Tasks/SshV0/ssh.ts) constructs SSH and script-copy connection options without `hostVerifier` or a known-hosts input. Its [helper](https://github.com/microsoft/azure-pipelines-tasks/blob/b7a1e04faa09a7028a67c9ac6d8c60c78153a6da/Tasks/SshV0/ssh2helpers.ts#L121-L159) passes those options to the SSH/SFTP clients.
- [`CopyFilesOverSSH@0` source, version 0.280.0](https://github.com/microsoft/azure-pipelines-tasks/blob/b7a1e04faa09a7028a67c9ac6d8c60c78153a6da/Tasks/CopyFilesOverSSHV0/copyfilesoverssh.ts) likewise omits a verifier in both execution paths. Its [helper](https://github.com/microsoft/azure-pipelines-tasks/blob/b7a1e04faa09a7028a67c9ac6d8c60c78153a6da/Tasks/CopyFilesOverSSHV0/sshhelper.ts#L31-L46) forwards the configuration unchanged.
- Their declared `ssh2` dependency range starts at `^1.15.0`. The [pinned 1.15.0 documentation](https://github.com/mscdex/ssh2/blob/a56e70e68efd6a1c7e665c9a473eb68d25571a55/README.md#L953-L955) specifies automatic acceptance when `hostVerifier` is unset. The SFTP client exposes the same optional setting. Installing an OpenSSH `known_hosts` file does not configure these separate Node clients.

This is **public source review**, not examination of the organization's installed task packages or a live interception test. Do not claim a pipeline is host-pinned because controller SSH, `ssh-keyscan` or an earlier independent probe succeeded. Do not add an unsupported task input, accept unknown keys or disable host checking to manufacture progress.

The new [transport source and acceptance runbook](transport/README.md) retain both required native tasks behind a strictly host-pinned OpenSSH loopback tunnel. A separate forwarding-only account/key, root-owned host profile, exact connection-UUID binding, per-job user service, bounded lifetime and always-run cleanup are implemented with offline tests. This is not a claim that the Node tasks now implement host verification themselves. Endpoint metadata, real SSH restrictions, correct/wrong-key behavior, timeout/cancellation cleanup and native-task routing **must still be verified live** on the approved images. There is no currently installed transport, agent or target, and this live gate remains open.

## Target contract — implement through reviewed Terraform and Ansible

The pipeline/helper files do **not** provision or configure a VM. Separate [Terraform web-target definitions](target/terraform/README.md) and a [human-operated Ansible target playbook, input validator and runbook](target/README.md) implement the infrastructure/configuration source below. Testing is **offline only**: shared Terraform guard and both AWS/Azure schema/mock-plan checks passed. The AWS provider was restored into an ignored private cache only after explicit download permission. Running these against approved accounts/hosts would create paid resources and change packages/account/Nginx/service configuration. Neither pipeline invokes them. Actual target provisioning/configuration and live validation remain pending.

Use **different A2 and A3 targets**: both serve `/var/www/html`, so using the same host would overwrite A2's grading site.

The [Week 08 AWS reference](../../week-08-terraform/terraform-aws-vm/README.md) is a useful starting point, **not a drop-in A2 target**. It bootstraps Nginx through cloud-init rather than the required Ansible, permits SSH from one controller `/32` rather than also the selected agent's approved egress `/32`, and does not create the dedicated deployment account/marker below. The [Week 08 Azure React reference](../../week-08-terraform/terraform-react-azure/README.md) builds on the VM through cloud-init, whereas A3 must build/test/publish on the pipeline agent and deploy only the artifact. The new narrow Terraform roots remove both bootstrap mismatches and bind SSH to reviewed controller/agent sources; Ansible owns the account/marker. Neither older stack nor its state was modified or presumed live. Do not blindly apply either reference, copy its state, or broaden SSH to the Internet.

Required Ansible-managed target state: verify the provisioning configuration and run the narrower runtime preflight before copying. The script checks the account name and nonzero UID, not its full privilege set. Review group memberships, sudoers and SSH authentication separately; passing the script is not proof of those controls.

- Dedicated Ubuntu target with Bash, GNU coreutils/find, curl, systemd and active Nginx. Public HTTP is a temporary non-sensitive lab endpoint, not a production security recommendation.
- Inner SSH key authentication as **`week10deploy`**, a dedicated non-root account with **no sudo/privileged groups**, accepted only from target loopback. The separate `week10tunnel` key accepts only the actual agent egress `/32` and permits local TCP forwarding solely to `127.0.0.1:22`; no shell, SFTP, remote forwarding or sudo. Do not give a pipeline the VM administrator's key. The service connection's private key/passphrase stays in its approved secret store, never YAML, arguments, environment exports, logs or screenshots.
- `/var/www` is `root:root`, mode `0755`; neither it nor `/var` is a symlink. `/var/www/html` is a real directory, writable by `week10deploy`, readable by Nginx, and contains no other application, symlinks, special files, raw `src/`, or `node_modules/`. Review existing contents before the first run; the preflight is not a comprehensive remote secret scanner.
- `/var/www/.dmi-week10-target` is a **regular, non-symlink file, owner/group `root:root`, mode `0644`**. Its sole line is `week10-a2` for static or `week10-a3` for React. Create it through the reviewed Ansible configuration, not by these pipeline tasks. It reduces accidental cross-target deployment; it is not SSH host authentication.
- Verify the host fingerprint through a trusted cloud/console channel before configuring SSH. Do not use TOFU, `StrictHostKeyChecking=no`, a private-key printout, or a keyscan alone as proof. These templates do not claim the Azure tasks enforce an OpenSSH known-hosts file; review the actual task/service-connection host-authentication behavior before live use. If the required assurance is unavailable, stop rather than describe it as pinned.
- HTTP routes to the exact web root. Static should return genuine 404s for missing content. React must serve the index for application routes but not missing static assets. The relevant Nginx locations for the latter are:

  ```nginx
  location ^~ /static/ {
      try_files $uri =404;
  }
  location / {
      try_files $uri $uri/ /index.html;
  }
  ```

The payload/remote verification helpers do not create accounts, change sudo, install packages, configure Nginx or mutate cloud resources. The separate transport helper **does start/check/stop one job-scoped unprivileged user service when invoked live**; its JSON validation modes are read-only. The Ansible playbooks are separately authorized host-changing operations.

## Artifact and deployment behavior

- The Python helper is read-only. It rejects symlinks, special files, unexpected directories/files, source maps and oversized payloads. It checks static body text or React build shape/name/date **presence**, not visual rendering or factual date correctness. Human review and a real browser check remain necessary.
- Build emits a deterministic SHA-256 manifest via stdout, which the pipeline writes outside the payload before moving it into place. Each later artifact boundary rechecks it. Unknown assets require a reviewed allowlist change, not disabling validation.
- React Build publishes **`candidate-site`**. Test independently checks out the same run's commit and installs its lockfile; it does not assume the same agent/workspace survives between stages. Publish downloads that candidate from the **current run**, verifies it after Test succeeds, and promotes **`site-build`**. Deploy downloads only that artifact, verifies it again and requires `refs/heads/main`. No raw React source, dependencies or source maps are uploaded.
- `npm ci --ignore-scripts` avoids dependency lifecycle hooks; the intended build/test runners are invoked explicitly. App/npm commands are prospective pipeline behavior only: none ran on this workstation during preparation. Dependencies still require review; these flags are not a sandbox.
- Copy uses the exact artifact root, fails on empty input and does **not** recursively clean the target. Old content-hashed assets can remain. Deployment is **not atomic** and automatic rollback is not implemented; use a controlled lab window and review retention/cleanup through Ansible. Do not share the target with another application.
- Remote verification is unprivileged: checksum validation, active Nginx, byte-for-byte local HTTP index comparison, React fallback and genuine static 404 checks, and a top-level web-root listing. It does not prove external firewall reachability or client-side rendering. `enableRemoteVsoCommands: false` prevents remote output from issuing Azure logging commands on the documented current SSH task.
- None of these scripts invokes Terraform, cloud APIs, agent registration or PAT handling. Only `ssh_tunnel.py` manages the job-scoped user tunnel service, under the [transport contract](transport/README.md#job-lifecycle-and-cleanup); it never restarts SSH/Nginx or manages cloud cleanup. A failed check stops the job/stage; there is no success-on-error shortcut.

## Offline checks

From this assessment repository's root, using the existing system Python, Bash, Ruby/Psych and the already available Terraform 1.13.5 executable at `.tools/terraform`:

```sh
env -i PATH=/usr/bin:/bin HOME=/nonexistent PYTHONDONTWRITEBYTECODE=1 \
  /usr/bin/sandbox-exec \
  -p '(version 1) (allow default) (deny network*) (deny file-write*)' \
  /usr/bin/python3 -I -B -m unittest discover \
  -s week-10-azure-devops/application-pipelines/tests -v
```

For the transport and 24-hour-window increment, **201 Python tests passed**: 137 application/source, 43 A1/preservation and 21 Azure agent plan/receipt tests, with networking and filesystem writes denied. Separately, both Ansible playbooks passed native syntax checks and **six controller-only rejection cases** (root operator, mismatched IP, missing trusted-host file for each playbook) exited at their expected guard with zero changes. These used synthetic inventories, a localhost-only limit, no SSH/sudo or IP networking, and writes restricted to cleaned invocation scratch. The shared Terraform guard passed **20 provider-free plan fixtures**, including an exact 24-hour window and rejection of longer windows. No new cloud-provider mock plans or real plans were run for this increment. These include 33 new transport tests plus the distinct target-key test: public input rejection, root-owned path checks, private-key non-reading, fixed OpenSSH options, credential-free subprocess environments, job/endpoint binding, readiness/cleanup failures, lifecycle wiring and real YAML parsing. All transport/systemd/SSH interactions in the Python suite are mocked; none starts an SSH/systemd service or contacts a host. The earlier import-receipt increment passed **103 application/source tests plus 43 A1/preservation tests** under the same restrictions. The suite covers pinned-input rejection, the name-only transformation, bounded read-only input, safe CLI rejection, allowlisted receipt metadata and the distinction between working reads, specific repository writes and unperformed deployment/evidence. The nine session-only operation tests are recorded separately in the import receipt. These are local checks, not application build/test runs or Azure pipeline results; Terraform plan fixtures were not rerun for that earlier receipt-only increment.

The suite parses both YAML files with real Psych, checks pipeline dependencies/permissions/artifact wiring, validates Bash syntax, tests payload/manifest rejection paths and runs the remote verifier **only with in-process shell stubs**. Fixtures are in-memory and explicitly synthetic; they are not React builds, deployments, pipeline logs or evidence. No test creates a file/account/service, contacts a host, installs a package, or executes instructor JavaScript. Azure's service-side schema validation, real npm tests, task execution and all live proof remain pending.

## Remaining Week 10 gates

A1's [later manual run 2 and cleanup](../self-hosted-agent/runtime-2026-09-19-interactive.json) were verified on 19 September 2026; that temporary agent/VM were removed. All seven A1 captures are published and their review is user-attested. Its assistant-drafted reflection is now separately user-approved, but PAT compliance and the universal secrecy assertion remain pending. A fresh approved Online agent is needed for A2/A3 runs; do not repeat A1 solely to fill already captured image slots. A2/A3 need scoped deployment identities, approved real Terraform/Ansible execution and all live and human requirements above. A4 additionally requires its actual application/configuration review, two repositories, a separately approved two-VM/private-MySQL Azure design, protected remote state/connections, approved infrastructure pipeline and manual output handoff. A5 requires healthy A4 pipelines and the **actual supplied kit**; the previously inspected nontruncated instructor main tree contained no supplied `CLAUDE.md`, `pipeline-triage.sh` or `.claude/skills/pipeline-triage/SKILL.md`. Do not manufacture the kit, incidents, reports, learner explanations, screenshots or LinkedIn posts. All original task and checklist wording is preserved; only the ten documented evidence/approval-backed answers have been checked.
